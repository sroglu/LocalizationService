# LocalizationService

Key→text localization: pluggable source, language selection, formatting, and a CSV/Sheets editor pipeline.

Part of the PFound modular Unity foundation. The Unity layer reuses the standalone PFound.Compression codec (referenced by assembly name).
